# Dotfiles

This directory contains my personal dotfiles, managed with GNU Stow.
Each subdirectory is a package, for example:
- zsh/ → .zshrc and related files
- vim/ → .vimrc and .vim folder
- tmux/ → .tmux.conf

